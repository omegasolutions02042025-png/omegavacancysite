# Модуль валют и ставок кандидатов - Руководство пользователя

## 📋 Обзор

Модуль автоматически конвертирует ставку кандидата во все поддерживаемые валюты (RUB, USD, EUR, BYN) на основе актуальных курсов ЦБ РФ.

## 🎯 Где отображаются ставки

### 1. Список кандидатов (`/candidate`)

На странице со всеми кандидатами под информацией о каждом кандидате отображаются его ставки во всех валютах:

```
💰 Ставка (monthly):
₽ 250,000 RUB  $ 2,617 USD  € 2,421 EUR  Br 8,547 BYN
```

### 2. Профиль кандидата (`/candidate/edit/{id}`)

В профиле кандидата есть отдельный раздел **"Ставки и валюты"** с:
- Основной ставкой кандидата (крупным шрифтом)
- Конвертированными значениями во всех валютах
- Датой последнего расчета
- Кнопкой для пересчета с актуальным курсом

### 3. Админ-панель (`/admin/candidates`)

В таблице всех кандидатов есть колонка со ставками в разных валютах.

## 🔧 Как использовать

### Установка ставки кандидата

1. **Через UI** - откройте профиль кандидата и заполните поле "Желаемая зарплата в USD"
2. **Через API** - используйте endpoint:

```bash
PUT /api/currency/candidates/{candidate_id}/rate
Content-Type: application/json

{
  "base_amount": 3000,
  "base_currency": "USD",
  "rate_type": "monthly"
}
```

### Пересчет ставок с актуальным курсом

#### Для одного кандидата:

**Через UI**: Нажмите кнопку "🔄 Пересчитать" в разделе "Ставки и валюты"

**Через API**:
```bash
POST /api/currency/candidates/{candidate_id}/rate/recalculate
```

#### Для всех кандидатов:

```bash
POST /api/currency/candidates/recalculate-all
```

### Получение текущих курсов валют

```bash
GET /api/currency/rates/current
```

Ответ:
```json
{
  "id": 1,
  "usd_rate": 95.50,
  "eur_rate": 103.25,
  "byn_rate": 29.80,
  "fetched_at": "2025-12-01T09:00:00",
  "is_active": true,
  "last_update_status": "success"
}
```

### Ручное обновление курсов

```bash
POST /api/currency/rates/refresh
```

## 📊 Поддерживаемые валюты

- **RUB** 🇷🇺 - Российский рубль (базовая валюта системы)
- **USD** 🇺🇸 - Доллар США
- **EUR** 🇪🇺 - Евро
- **BYN** 🇧🇾 - Белорусский рубль

## 🔄 Автоматическое обновление курсов

Курсы валют обновляются автоматически:
- ✅ При запуске приложения
- ✅ Каждый день в 09:00 (МСК)

Источник: Центральный Банк Российской Федерации (cbr.ru)

## 💡 Примеры использования

### Пример 1: Кандидат с почасовой ставкой в USD

```json
{
  "base_amount": 50,
  "base_currency": "USD",
  "rate_type": "hourly"
}
```

Результат:
- ₽ 4,775 RUB
- $ 50 USD
- € 46.25 EUR
- Br 162 BYN

### Пример 2: Кандидат с месячной ставкой в RUB

```json
{
  "base_amount": 250000,
  "base_currency": "RUB",
  "rate_type": "monthly"
}
```

Результат:
- ₽ 250,000 RUB
- $ 2,617 USD
- € 2,421 EUR
- Br 8,547 BYN

### Пример 3: Кандидат с годовой ставкой в EUR

```json
{
  "base_amount": 60000,
  "base_currency": "EUR",
  "rate_type": "yearly"
}
```

Результат:
- ₽ 6,195,000 RUB
- $ 64,869 USD
- € 60,000 EUR
- Br 211,800 BYN

## 🎨 Визуальные индикаторы

- **Базовая валюта** выделена цветной рамкой
- **Актуальность данных** показана внизу виджета
- **Тип ставки** отображается иконкой:
  - 💼 Почасовая
  - 📆 Месячная
  - 📅 Годовая

## ⚠️ Важные замечания

1. **Первичная установка**: При первом добавлении кандидата ставка не устанавливается автоматически
2. **Обновление курсов**: Если курсы не удалось получить от ЦБ РФ, используются последние сохраненные
3. **Округление**: Все значения округляются до 2 знаков после запятой
4. **Пересчет**: Рекомендуется пересчитывать ставки после значительных изменений курсов валют

## 🔗 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/currency/rates/current` | Получить текущие курсы |
| POST | `/api/currency/rates/refresh` | Обновить курсы вручную |
| POST | `/api/currency/convert` | Конвертировать валюту |
| POST | `/api/currency/calculate-rates` | Рассчитать ставки |
| GET | `/api/currency/candidates/{id}/rate` | Получить ставку кандидата |
| PUT | `/api/currency/candidates/{id}/rate` | Обновить ставку кандидата |
| POST | `/api/currency/candidates/{id}/rate/recalculate` | Пересчитать ставку |
| POST | `/api/currency/candidates/recalculate-all` | Пересчитать все ставки |

## 📝 Swagger документация

Полная интерактивная документация доступна по адресу:
```
http://localhost:8000/docs
```

В разделе **currency** вы найдете все эндпоинты с возможностью их протестировать.

---

**Готово!** Теперь вы можете эффективно управлять ставками кандидатов в разных валютах! 💰🚀

