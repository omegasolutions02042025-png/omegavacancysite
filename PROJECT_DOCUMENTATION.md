# Документация проекта OmegaVac Site

## 📋 Оглавление

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура](#архитектура)
3. [Структура проекта](#структура-проекта)
4. [Технологический стек](#технологический-стек)
5. [Модели данных](#модели-данных)
6. [API эндпоинты](#api-эндпоинты)
7. [Модули и компоненты](#модули-и-компоненты)
8. [База данных](#база-данных)
9. [Миграции](#миграции)
10. [Конфигурация](#конфигурация)
11. [Запуск и развертывание](#запуск-и-развертывание)

---

## Обзор проекта

**OmegaVac Site** — это веб-приложение на FastAPI для управления рекрутинговым процессом. Система предназначена для:

- Управления кандидатами и их профилями
- Работы с вакансиями
- Проведения сверок (сопоставления кандидатов с вакансиями)
- Коммуникации через Telegram и Email
- Управления пользователями и ролями
- Обработки резюме с использованием AI (GPT/Gemini)
- Управления курсами валют и ставками кандидатов

### Основные функции

- **Аутентификация и авторизация**: Система ролей (Admin, Recruiter, Candidate, Contractor)
- **Управление кандидатами**: Парсинг резюме, создание профилей, управление статусами
- **Работа с вакансиями**: Создание, редактирование, фильтрация вакансий
- **Сверки**: Автоматическое сопоставление кандидатов с вакансиями
- **Коммуникация**: Интеграция с Telegram и Email для общения с кандидатами
- **Модуль валют**: Автоматический пересчет ставок кандидатов в разных валютах
- **Административная панель**: Управление пользователями, заявками на регистрацию

---

## Архитектура

Проект следует многослойной архитектуре:

```
┌─────────────────────────────────────────┐
│         HTTP Requests (FastAPI)         │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│         Routes Layer (app/routes/)       │
│  - Обработка HTTP запросов              │
│  - Валидация входных данных             │
│  - Возврат HTML/JSON ответов            │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│    Services Layer (app/services/)       │
│  - Бизнес-логика                        │
│  - Обработка файлов                     │
│  - Интеграции с внешними API            │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│   Database Layer (app/database/)        │
│  - Репозитории для работы с БД          │
│  - CRUD операции                        │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│      Models Layer (app/models/)         │
│  - SQLModel модели                      │
│  - Определение структуры таблиц         │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│         PostgreSQL Database              │
└─────────────────────────────────────────┘
```

### Принципы архитектуры

1. **Разделение ответственности**: Каждый слой имеет четкую роль
2. **Dependency Injection**: Использование FastAPI Depends для внедрения зависимостей
3. **Асинхронность**: Все операции с БД асинхронные (async/await)
4. **Типобезопасность**: Использование type hints и Pydantic для валидации

---

## Структура проекта

```
omegavacsite/
├── app/                          # Основной пакет приложения
│   ├── __init__.py
│   ├── main.py                   # Точка входа FastAPI приложения
│   │
│   ├── core/                     # Ядро приложения
│   │   ├── config.py             # Настройки (Pydantic Settings)
│   │   ├── database.py           # SQLModel engine и модели БД
│   │   ├── security.py           # JWT, пароли, безопасность
│   │   ├── passwords.py          # Хеширование паролей
│   │   ├── current_user.py       # Получение текущего пользователя
│   │   ├── email_send.py         # Отправка email через SMTP
│   │   ├── email_listener.py     # Прослушивание входящих email
│   │   ├── gpt.py                # Интеграция с OpenAI GPT
│   │   ├── promts.py             # Промпты для GPT
│   │   ├── telethon_check.py     # Интеграция с Telegram (Telethon)
│   │   ├── scheduler.py          # Планировщик задач (APScheduler)
│   │   ├── exchange_rate_parser.py # Парсинг курсов валют
│   │   ├── generate_wl_resume.py # Генерация WhiteLabel резюме
│   │   ├── chat_websocket.py     # WebSocket для чата
│   │   ├── websocket_notif.py    # WebSocket уведомления
│   │   ├── redis.py              # Redis клиент
│   │   ├── cleanup.py            # Очистка временных файлов
│   │   └── utils.py              # Утилиты
│   │
│   ├── models/                   # SQLModel модели
│   │   ├── user.py               # Модели пользователей
│   │   ├── candidate.py          # Модели кандидатов
│   │   ├── vacancy.py            # Модели вакансий
│   │   ├── exchange_rate.py      # Модели курсов валют
│   │   ├── token.py              # Модели токенов
│   │   ├── password.py           # Модели паролей
│   │   ├── send_mail.py          # Модели отправки почты
│   │   ├── upload.py             # Модели загрузки файлов
│   │   └── dropdown.py           # Модели выпадающих списков
│   │
│   ├── database/                 # Репозитории БД
│   │   ├── database.py           # Настройка БД и сессий
│   │   ├── user_db.py            # UserRepository
│   │   ├── admin_db.py           # AdminRepository
│   │   ├── candidate_db.py       # CandidateRepository
│   │   ├── candidate_profile_db.py # CandidateProfileRepository
│   │   ├── vacancy_db.py         # VacancyRepository
│   │   ├── registration_db.py    # RegistrationRepository
│   │   ├── chat_db.py            # ChatRepository
│   │   └── dropdown_db.py        # DropdownRepository
│   │
│   ├── routes/                   # HTTP роутеры
│   │   ├── auth.py               # Аутентификация
│   │   ├── admin.py              # Административная панель
│   │   ├── candidate.py          # Управление кандидатами
│   │   ├── vacancy.py            # Управление вакансиями
│   │   ├── sverka.py             # Сверки
│   │   ├── chat.py               # Чат
│   │   ├── upload.py             # Загрузка файлов
│   │   ├── currency.py           # Модуль валют
│   │   ├── email.py              # Email функциональность
│   │   ├── send_mails.py         # Отправка писем
│   │   ├── telegram_link.py      # Интеграция Telegram
│   │   ├── photo.py              # Работа с фото
│   │   ├── dropdown.py           # Выпадающие списки
│   │   ├── download_wl.py        # Скачивание WhiteLabel резюме
│   │   ├── notify.py             # Уведомления
│   │   ├── websock.py            # WebSocket
│   │   └── items.py              # CRUD для items (пример)
│   │
│   ├── services/                 # Бизнес-логика
│   │   ├── currency_service.py   # Сервис валют
│   │   └── upload_service.py     # Сервис загрузки файлов
│   │
│   ├── dependencies/             # Зависимости FastAPI
│   │   └── auth.py               # Зависимости аутентификации
│   │
│   ├── templates/                # Jinja2 шаблоны
│   │   ├── layouts/             # Базовые шаблоны
│   │   ├── auth/                # Страницы аутентификации
│   │   ├── admin/               # Административная панель
│   │   ├── candidate/           # Страницы кандидатов
│   │   ├── vacancy/             # Страницы вакансий
│   │   ├── sverka/              # Страницы сверок
│   │   ├── chat/                # Страницы чата
│   │   ├── upload/              # Страницы загрузки
│   │   └── components/          # Компоненты UI
│   │
│   ├── static/                  # Статические файлы
│   │   ├── css/                 # Стили
│   │   ├── img/                 # Изображения
│   │   └── sounds/              # Звуки
│   │
│   ├── media/                   # Медиа файлы
│   │   ├── chat/                # Файлы из чата
│   │   ├── email/               # Файлы из email
│   │   └── photos/              # Фото пользователей
│   │
│   └── sessions/                # Telegram сессии
│
├── alembic/                     # Миграции БД
│   ├── env.py
│   └── versions/                # Файлы миграций
│
├── scripts/                      # Вспомогательные скрипты
│   ├── create_admin.py          # Создание администратора
│   ├── migrate_*.py             # Скрипты миграций
│   └── ...
│
├── migrations/                  # SQL миграции
│
├── resumes/                     # Загруженные резюме
│
├── uploaded_resumes/            # Обработанные резюме
│
├── WhiteLabel_Resume/           # Сгенерированные WhiteLabel резюме
│
├── politic/                     # Политики конфиденциальности
│
├── requirements.txt             # Зависимости Python
├── alembic.ini                  # Конфигурация Alembic
├── main.py                      # Entrypoint (импортирует app.main)
└── README.md                    # Основная документация
```

---

## Технологический стек

### Backend
- **FastAPI** 0.121.0 - Веб-фреймворк
- **SQLModel** 0.0.27 - ORM (SQLAlchemy + Pydantic)
- **SQLAlchemy** 2.0.44 - Работа с БД
- **PostgreSQL** (asyncpg) - База данных
- **Alembic** 1.17.2 - Миграции БД

### Аутентификация и безопасность
- **python-jose** 3.5.0 - JWT токены
- **passlib** 1.7.4 - Хеширование паролей
- **bcrypt** 5.0.0 - Алгоритм хеширования

### Интеграции
- **Telethon** 1.42.0 - Telegram API
- **aiosmtplib** 5.0.0 - Асинхронная отправка email
- **imap-tools** 1.11.0 - Чтение входящих email
- **openai** 2.7.2 - OpenAI GPT API
- **google-generativeai** 0.8.5 - Google Gemini API

### Обработка файлов
- **python-docx** 1.2.0 - Работа с Word документами
- **PyPDF2** 3.0.1 - Работа с PDF
- **textract** 1.6.5 - Извлечение текста из файлов
- **Pillow** 12.0.0 - Обработка изображений

### Планировщик и фоновые задачи
- **APScheduler** 3.11.1 - Планировщик задач
- **redis** 7.0.1 - Кэширование и очереди

### Утилиты
- **python-dotenv** 1.2.1 - Загрузка переменных окружения
- **pydantic** 2.12.3 - Валидация данных
- **pydantic-settings** 2.11.0 - Настройки из .env

---

## Модели данных

### Основные модели

#### User (Пользователь)
```python
- id: int (PK)
- email: str (unique)
- password: str
- role: UserRole (CANDIDATE, RECRUITER, CONTRACTOR, ADMIN)
- first_name, last_name, middle_name: str
- phone: str
- work_telegram: str
- work_email: str
- work_telegram_session_name: str
- work_email_app_pass: str
- photo_path: str
- is_archived: bool
- archived_at: str
- created_at: str
- pd_consent: bool (согласие на обработку ПД)
```

#### RecruiterCandidates (Профиль кандидата)
```python
- id: int (PK)
- user_id: int (FK -> users.id)
- number_for_user: int
- first_name, last_name, middle_name: str
- title: str
- email, telegram, phone: str
- linkedin, github, portfolio: str
- about: str
- photo_path: str
- salary_usd: float
- base_rate_amount: float
- base_rate_currency: str (RUB, USD, EUR, BYN)
- rate_type: str (hourly, monthly, yearly)
- rate_rub, rate_usd, rate_eur, rate_byn: float
- grade: str
- work_format: str
- employment_type: str
- company_types: str
- specializations: str
- skills: str
- city: str
- timezone: str
- regions, countries: str
- relocation: str
- experience: JSON (список словарей)
- education: JSON (список словарей)
- courses: JSON (список словарей)
- projects: JSON (список словарей)
- english_level: str
- status: CandidateStatus
- status_until: str
- created_at: str
```

#### Vacancy (Вакансия)
```python
- id: int (PK)
- vacancy_id: str (unique)
- title: str
- vacancy_text: str
- work_format: str
- employment_type: str
- english_level: str
- grade: str
- company_type: str
- specializations: str
- skills: str
- domains: str
- manager_username: str
- location: str
- customer: str
- categories: str
- subcategories: str
- salary: str
- created_at: str
```

#### Sverka (Сверка)
```python
- id: int (PK)
- user_id: int (FK -> users.id)
- vacancy_id: str
- slug: str
- sverka_json: JSON
- candidate_fullname: str
```

#### Chat (Сообщение чата)
```python
- id: int (PK)
- user_id: int (FK -> users.id)
- candidate_id: int
- candidate_fullname: str
- vacancy_id: str
- vacancy_title: str
- message_type: str (telegram/email)
- sender: str (user/candidate)
- message_text: str
- timestamp: str
- is_read: bool
- has_media: bool
- media_type: str
- media_path: str
- media_filename: str
```

#### ExchangeRate (Курс валют)
```python
- id: int (PK)
- usd_rate: float
- eur_rate: float
- byn_rate: float
- fetched_at: str
- is_active: bool
- last_update_status: str
- error_message: str
```

#### RegistrationRequest (Заявка на регистрацию)
```python
- id: int (PK)
- email: str (unique)
- password: str
- first_name, last_name, middle_name: str
- phone: str
- specialization: str
- experience: str
- resume: str
- verification_token: str
- is_email_verified: bool
- is_approved: bool (None=pending, True=approved, False=rejected)
- role: UserRole
- pd_consent: bool
- created_at: str
```

#### Admin (Администратор)
```python
- id: int (PK)
- username: str (unique)
- hashed_password: str
- photo_path: str
- created_at: str
```

### Enums

#### UserRole
- `CANDIDATE` - Кандидат
- `RECRUITER` - Рекрутер
- `CONTRACTOR` - Подрядчик
- `ADMIN` - Администратор

#### CandidateStatus
- `В активном поиске`
- `Рассматривает предложения`
- `Не ищет работу`
- `Временно неактивен`

---

## API эндпоинты

### Аутентификация (`/auth/`)
- `GET /auth/login` - Страница входа
- `POST /auth/login` - Вход пользователя
- `GET /auth/logout` - Выход
- `GET /auth/register` - Страница регистрации
- `POST /auth/register` - Подача заявки на регистрацию
- `GET /auth/verify-email/{token}` - Подтверждение email
- `GET /auth/forgot-password` - Страница восстановления пароля
- `POST /auth/reset-password` - Сброс пароля
- `GET /auth/profile` - Профиль пользователя

### Административная панель (`/admin/`)
- `GET /admin/login` - Страница входа администратора
- `POST /admin/login` - Вход администратора
- `GET /admin/dashboard` - Панель администратора
- `GET /admin/pending-registrations` - Заявки на регистрацию
- `POST /admin/approve-registration/{id}` - Одобрить заявку
- `POST /admin/reject-registration/{id}` - Отклонить заявку
- `POST /admin/create-recruiter` - Создать рекрутера
- `GET /admin/candidates` - Все кандидаты
- `GET /admin/sverkas` - Все сверки

### Кандидаты (`/candidate/`)
- `GET /candidate/` - Список кандидатов
- `GET /candidate/add` - Страница добавления кандидата
- `POST /candidate/add` - Добавить кандидата
- `GET /candidate/{number}` - Профиль кандидата
- `GET /candidate/{number}/open` - Открыть кандидата
- `POST /candidate/{number}/update` - Обновить кандидата
- `POST /candidate/upload` - Загрузить резюме
- `GET /candidate/upload/status/{task_id}` - Статус обработки

### Вакансии (`/vacancy/`)
- `GET /vacancy/` - Список вакансий
- `GET /vacancy/start` - Страница создания вакансии
- `POST /vacancy/create` - Создать вакансию
- `GET /vacancy/{vacancy_id}` - Детали вакансии

### Сверки (`/sverka/`)
- `GET /sverka/start` - Страница начала сверки
- `POST /sverka/from-candidate` - Сверка от кандидата
- `GET /sverka/history` - История сверок
- `GET /sverka/result/{slug}` - Результат сверки
- `GET /sverka/status/{task_id}` - Статус обработки

### Чат (`/chat/`)
- `GET /chat/` - Список чатов
- `GET /chat/{candidate_id}` - Чат с кандидатом
- `POST /chat/send` - Отправить сообщение
- `GET /chat/messages/{candidate_id}` - Сообщения чата

### Загрузка файлов (`/upload/`)
- `GET /upload` - Страница загрузки
- `POST /upload` - Загрузить файл
- `GET /upload/status/{task_id}` - Статус обработки

### Валюты (`/api/currency/`)
- `GET /api/currency/rates/current` - Текущие курсы
- `POST /api/currency/rates/refresh` - Обновить курсы (админ)
- `POST /api/currency/convert` - Конвертировать валюту
- `POST /api/currency/calculate-rates` - Рассчитать ставку
- `GET /api/currency/candidates/{id}/rate` - Ставка кандидата
- `PUT /api/currency/candidates/{id}/rate` - Обновить ставку
- `POST /api/currency/candidates/{id}/rate/recalculate` - Пересчитать ставку
- `POST /api/currency/candidates/recalculate-all` - Пересчитать все (админ)

### Email (`/email/`)
- `GET /email/` - Страница email
- `POST /email/send` - Отправить email

### Telegram (`/telegram/`)
- `GET /telegram/link` - Страница привязки Telegram
- `POST /telegram/link` - Привязать Telegram

### WebSocket
- `WS /ws/notifications` - Уведомления
- `WS /ws/chat` - Чат в реальном времени

---

## Модули и компоненты

### Core модули

#### `app/core/config.py`
Настройки приложения через Pydantic Settings:
- Database URL
- API ключи (OpenAI, Gemini, Telegram)
- SMTP настройки
- JWT настройки

#### `app/core/security.py`
Безопасность:
- Генерация JWT токенов
- Валидация токенов
- Настройки cookies

#### `app/core/passwords.py`
Хеширование паролей:
- `hash_password()` - Хеширование
- `verify_password()` - Проверка

#### `app/core/email_send.py`
Отправка email:
- `send_email_smtp()` - Универсальная отправка через SMTP
- Поддержка HTML и plain text

#### `app/core/email_listener.py`
Прослушивание входящих email:
- Подключение к IMAP
- Парсинг писем
- Сохранение в чат

#### `app/core/gpt.py`
Интеграция с AI:
- Парсинг резюме через GPT
- Генерация профилей кандидатов

#### `app/core/telethon_check.py`
Интеграция с Telegram:
- Управление сессиями
- Отправка/получение сообщений
- Синхронизация с чатом

#### `app/core/scheduler.py`
Планировщик задач:
- Обновление курсов валют (ежедневно в 09:00)
- Очистка временных файлов
- Другие периодические задачи

#### `app/core/exchange_rate_parser.py`
Парсинг курсов валют:
- Получение курсов с сайта ЦБ РФ
- Сохранение в БД

### Database репозитории

#### `app/database/user_db.py` - UserRepository
- `get_by_email()` - Получить по email
- `create_user()` - Создать пользователя
- `authenticate()` - Аутентификация
- `update_user_telegram()` - Обновить Telegram
- `update_user_email()` - Обновить email
- `reset_password()` - Сброс пароля
- `get_user_sessions()` - Активные сессии
- `get_chat_id_candidates()` - Telegram ID кандидатов
- `get_user_notifications()` - Уведомления

#### `app/database/candidate_db.py` - CandidateRepository
- `create_candidate()` - Создать кандидата
- `get_candidate_by_number()` - Получить по номеру
- `update_candidate()` - Обновить кандидата
- `get_all_candidates()` - Все кандидаты пользователя
- `delete_candidate()` - Удалить кандидата

#### `app/database/vacancy_db.py` - VacancyRepository
- `create_vacancy()` - Создать вакансию
- `get_vacancy_by_id()` - Получить по ID
- `get_all_vacancies()` - Все вакансии
- `update_vacancy()` - Обновить вакансию

#### `app/database/registration_db.py` - RegistrationRepository
- `create_request()` - Создать заявку
- `verify_email()` - Подтвердить email
- `get_pending_requests()` - Ожидающие заявки
- `approve_request()` - Одобрить заявку
- `reject_request()` - Отклонить заявку

### Services

#### `app/services/currency_service.py` - CurrencyService
- `convert_to_rub()` - Конвертация в рубли
- `convert_from_rub()` - Конвертация из рублей
- `calculate_all_rates()` - Расчет во всех валютах
- `update_exchange_rates()` - Обновление курсов
- `ensure_rates_available()` - Проверка доступности курсов

#### `app/services/upload_service.py` - UploadService
- `save_file()` - Сохранение файла
- `process_resume()` - Обработка резюме

---

## База данных

### Подключение
- **Тип**: PostgreSQL (asyncpg)
- **URL**: Настраивается в `app/core/config.py` через переменную `database_url`
- **По умолчанию**: `postgresql+asyncpg://postgres:123546@localhost:5432/omegasite`

### Основные таблицы
- `users` - Пользователи
- `admins` - Администраторы
- `candidate_profiles` - Профили кандидатов
- `vacancy` - Вакансии
- `sverka` - Сверки
- `chat` - Сообщения чата
- `deleted_chat` - Удаленные сообщения
- `user_comunication` - Связи пользователей с кандидатами
- `telegram_dialog_status` - Статусы Telegram диалогов
- `user_notitfication` - Уведомления
- `registration_requests` - Заявки на регистрацию
- `password_reset_tokens` - Токены сброса пароля
- `exchange_rates` - Курсы валют
- `skill_dropdown`, `specialization_dropdown`, `domain_dropdown` - Выпадающие списки
- `location_dropdown`, `manager_dropdown`, `customer_dropdown`
- `category_dropdown`, `subcategory_dropdown`
- `user_customer_access` - Доступы рекрутеров к заказчикам

### Связи (Relationships)
- User → RecruiterCandidates (One-to-Many)
- User → Sverka (One-to-Many)
- User → Chat (One-to-Many)
- User → UserComunication (One-to-Many)
- User → UserNotification (One-to-Many)
- User → TelegramDialogStatus (One-to-Many)

---

## Миграции

### Alembic
Проект использует Alembic для миграций БД.

#### Создание миграции
```bash
alembic revision --autogenerate -m "описание изменений"
```

#### Применение миграций
```bash
alembic upgrade head
```

#### Откат миграции
```bash
alembic downgrade -1
```

### Существующие миграции
- `add_user_roles_system` - Система ролей
- `add_password_changed_at` - Поле смены пароля
- `add_archive_status_fields` - Поля архивации
- `add_candidate_id_to_chat` - Связь кандидата с чатом
- `add_pd_consent_fields` - Поля согласия на обработку ПД
- `add_role_to_registration_requests` - Роли в заявках
- `add_created_at_to_candidates` - Дата создания кандидата

---

## Конфигурация

### Переменные окружения (.env)

```env
# База данных
DATABASE_URL=postgresql+asyncpg://postgres:123546@localhost:5432/omegasite

# Пути
SAVE_RESUME_PATH=./resumes

# API ключи
GEMINI_API_KEY=your_gemini_key
OPEN_AI_API_KEY=your_openai_key
SECRET_KEY=your_secret_key
API_ID=your_telegram_api_id
API_HASH=your_telegram_api_hash

# JWT
JWT_SECRET_KEY=your_jwt_secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# SMTP
SMTP_HOST=mailbe07.hoster.by
SMTP_PORT=465
SMTP_USERNAME=admin@omega-solutions.ru
SMTP_PASSWORD=Omegaadmin2025!
SMTP_FROM_EMAIL=admin@omega-solutions.ru
SMTP_USE_TLS=True
SMTP_USE_STARTTLS=False
```

### Настройки в `app/core/config.py`
Все настройки загружаются из `.env` файла через Pydantic Settings.

---

## Запуск и развертывание

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Настройка базы данных
1. Создать базу данных PostgreSQL
2. Настроить `DATABASE_URL` в `.env`
3. Применить миграции:
```bash
alembic upgrade head
```

### Создание первого администратора
```bash
python scripts/create_admin.py
```
или
```bash
python app/setup_admin.py
```

### Запуск приложения

#### Development
```bash
uvicorn main:app --reload
```

#### Production
```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

### Доступ к приложению
- **Главная страница**: http://127.0.0.1:8000/
- **API документация**: http://127.0.0.1:8000/docs
- **Админ панель**: http://127.0.0.1:8000/admin/login

### Фоновые задачи
При запуске приложения автоматически запускаются:
- Планировщик задач (APScheduler)
- Email listeners
- Telegram сессии

### Остановка
При остановке приложения автоматически:
- Останавливается планировщик
- Закрываются email listeners
- Закрываются Telegram сессии

---

## Дополнительная информация

### Документация по модулям
- `CURRENCY_MODULE.md` - Модуль валют
- `ARCHITECTURE.md` - Архитектура приложения
- `DOCUMENTATION_SUMMARY.md` - Сводка документации
- `ALEMBIC_GUIDE.md` - Руководство по миграциям
- `ADMIN_SETUP.md` - Настройка администратора

### Скрипты
- `scripts/create_admin.py` - Создание администратора
- `scripts/migrate_*.py` - Различные миграции
- `scripts/check_*.py` - Проверка состояния БД

### Важные замечания
1. **Безопасность**: Пароли пользователей хранятся в открытом виде для администратора (требуется доработка)
2. **Telegram**: Требуется настройка API_ID и API_HASH
3. **Email**: Требуется настройка SMTP сервера
4. **AI**: Требуются API ключи для GPT/Gemini

---

## Контакты и поддержка

Для вопросов и поддержки обращайтесь к команде разработки.

---

*Документация обновлена: 2025-12-05*

