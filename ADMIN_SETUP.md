# 🔐 Настройка панели администратора

## Создание первого администратора

### Вариант 1: Через API (Рекомендуется)

После запуска сервера выполните POST запрос:

```bash
curl -X POST http://localhost:8000/admin/setup-initial-admin
```

Или откройте в браузере и выполните через консоль (F12):

```javascript
fetch('/admin/setup-initial-admin', {method: 'POST'})
  .then(r => r.json())
  .then(data => console.log(data));
```

### Вариант 2: Через Python скрипт

```bash
python -c "from app.setup_admin import create_admin_sync; create_admin_sync()"
```

## Учетные данные администратора

После создания администратора учетные данные будут:

- **Логин:** `admin`
- **Пароль:** `OmegaAdmin2025!`
- **URL для входа:** 
  - http://localhost:8000/admin/login
  - http://omegahire.tech/admin/login

Учетные данные также сохраняются в файл `admin_credentials.txt` в корне проекта.

## Функции панели администратора

### 1. Создание аккаунтов рекрутеров

1. Войдите в панель администратора
2. Введите email нового рекрутера
3. Нажмите "Создать аккаунт"
4. **Сохраните сгенерированный пароль** - он больше не будет показан!

### 2. Просмотр списка рекрутеров

В панели отображаются все созданные аккаунты рекрутеров с:
- ID
- Email
- Датой создания

## Изменения в системе аутентификации

### ❌ Отключено:
- Публичная регистрация (`/auth/register`)
- Самостоятельное создание аккаунтов

### ✅ Включено:
- Создание аккаунтов только через админ-панель
- Автоматическая генерация безопасных паролей
- Отслеживание кто создал каждый аккаунт

## Безопасность

⚠️ **ВАЖНО:**
- Храните `admin_credentials.txt` в безопасном месте
- Не коммитьте этот файл в Git (добавлен в `.gitignore`)
- Измените пароль администратора после первого входа (функция будет добавлена)
- Сохраняйте пароли рекрутеров сразу после создания

## Структура базы данных

### Таблица `admins`
- `id` - ID администратора
- `username` - Логин
- `hashed_password` - Хешированный пароль
- `created_at` - Дата создания

### Таблица `users` (обновлена)
- Добавлено поле `created_by_admin` - ID администратора, создавшего аккаунт
- Добавлено поле `created_at` - Дата создания

## Troubleshooting

### Ошибка "Администратор уже существует"

Если вы пытаетесь создать администратора повторно, используйте существующие учетные данные из файла `admin_credentials.txt`.

### Забыли пароль администратора?

Если вы потеряли файл с учетными данными, вам нужно будет:
1. Удалить запись из таблицы `admins` в базе данных
2. Создать нового администратора через API

### Не можете войти в админ-панель?

Проверьте:
1. Правильность логина и пароля
2. Что сервер запущен
3. Что таблица `admins` создана в базе данных

