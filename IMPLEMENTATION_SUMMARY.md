# Система регистрации пользователей - Сводка изменений

## 📋 Обзор

Реализована полная система регистрации пользователей с подтверждением email и одобрением администратором.

## ✅ Выполненные задачи

### 1. База данных и модели

#### Новая таблица `registration_requests` (`app/database/database.py`)
- Хранит заявки на регистрацию
- Поля: email, пароль (открытый текст), токен верификации, статусы подтверждения email и одобрения
- Связь с администратором, который одобрил/отклонил

#### Изменения в таблице `users` (`app/database/database.py`)
- **КРИТИЧЕСКОЕ**: Поле `hashed_password` → `password` (теперь хранится в открытом виде)
- Это позволяет администратору просматривать пароли всех рекрутеров

#### Новый репозиторий `app/database/registration_db.py`
- `create_request()` - создать заявку
- `verify_email()` - подтвердить email по токену
- `get_pending_requests()` - получить заявки на рассмотрение
- `approve_request()` - одобрить и создать пользователя
- `reject_request()` - отклонить заявку

### 2. Обновление существующих репозиториев

#### `app/database/user_db.py`
- Убрано хеширование паролей
- Метод `authenticate()` теперь сравнивает пароли напрямую
- Метод `reset_password()` сохраняет пароль в открытом виде

#### `app/database/admin_db.py`
- `create_recruiter()` - теперь сохраняет пароль в открытом виде
- `get_all_candidates()` - новый метод для просмотра всех кандидатов
- `get_all_sverkas()` - новый метод для просмотра всех сверок

### 3. Конфигурация email (`app/core/config.py`)

Добавлены настройки SMTP:
- `smtp_host` - хост SMTP-сервера
- `smtp_port` - порт SMTP
- `smtp_username` - имя пользователя
- `smtp_password` - пароль SMTP
- `smtp_from_email` - email отправителя
- `smtp_use_tls` / `smtp_use_starttls` - флаги безопасности

### 4. Маршруты регистрации (`app/routes/auth.py`)

#### `GET /auth/register`
- Страница подачи заявки на регистрацию

#### `POST /auth/register`
- Создает заявку в БД
- Отправляет письмо с ссылкой для подтверждения email
- Показывает страницу успеха

#### `GET /auth/verify-email?token=...`
- Подтверждает email по токену
- Отправляет уведомление администратору о новой заявке
- Показывает страницу ожидания одобрения

### 5. Маршруты администратора (`app/routes/admin.py`)

#### `GET /admin/pending-registrations`
- Показывает все заявки ожидающие рассмотрения
- Отображает email и пароль каждой заявки

#### `POST /admin/approve-registration/{request_id}`
- Одобряет заявку
- Создает пользователя в системе
- Отправляет письмо пользователю с учетными данными

#### `POST /admin/reject-registration/{request_id}`
- Отклоняет заявку
- Отправляет письмо пользователю об отклонении

#### `GET /admin/candidates`
- Показывает всех кандидатов из всех рекрутеров

#### `GET /admin/sverkas`
- Показывает все сверки из всех рекрутеров

#### Обновлен `GET /admin/dashboard`
- Показывает счетчик заявок на регистрацию
- Добавлены карточки-ссылки на новые разделы
- Отображает пароли всех рекрутеров в таблице

### 6. HTML-шаблоны

#### `app/templates/auth/register.html`
- Форма подачи заявки на регистрацию
- Инфобокс с описанием процесса

#### `app/templates/auth/register_success.html`
- Страница успешной отправки заявки
- Инструкции следующих шагов

#### `app/templates/auth/email_verified.html`
- Страница после подтверждения email
- Уведомление об ожидании одобрения

#### `app/templates/auth/login.html`
- Добавлена ссылка "Подать заявку" вместо "Аккаунты создаются администратором"

#### `app/templates/admin/pending_registrations.html`
- Таблица заявок с кнопками одобрить/отклонить
- Отображает email, пароль, дату заявки

#### `app/templates/admin/candidates.html`
- Таблица всех кандидатов со всеми полями

#### `app/templates/admin/sverkas.html`
- Таблица всех сверок

#### `app/templates/admin/dashboard.html`
- Добавлены карточки-ссылки на:
  - Заявки на регистрацию (с счетчиком)
  - Все кандидаты
  - Все сверки
- В таблице рекрутеров добавлен столбец "Пароль"

## 🔄 Процесс регистрации

```
1. Пользователь → заполняет форму на /auth/register
   ↓
2. Система → создает заявку в БД, отправляет письмо с токеном
   ↓
3. Пользователь → переходит по ссылке из письма
   ↓
4. Система → подтверждает email, уведомляет администратора
   ↓
5. Администратор → видит заявку в /admin/pending-registrations
   ↓
6. Администратор → одобряет или отклоняет
   ↓
7. Система → создает пользователя (если одобрено), отправляет письмо с вердиктом
   ↓
8. Пользователь → получает письмо с учетными данными и может войти
```

## 📧 Email-уведомления

1. **Подтверждение email** (пользователю)
   - Отправитель: `zakaz@omega-solutions.ru`
   - Тема: "Подтверждение регистрации на OmegaHire"
   - Содержит ссылку для подтверждения email

2. **Уведомление администратору** (на `zakaz@omega-solutions.ru`)
   - Тема: "Новая заявка на регистрацию OmegaHire"
   - Содержит email и пароль пользователя
   - Ссылку на страницу рассмотрения заявок

3. **Одобрение заявки** (пользователю)
   - Тема: "Ваша заявка на OmegaHire одобрена"
   - Содержит email и пароль для входа
   - Ссылку на страницу входа

4. **Отклонение заявки** (пользователю)
   - Тема: "Ваша заявка на OmegaHire отклонена"
   - Информация о контакте для вопросов

## 🔒 Безопасность

### ⚠️ Важные замечания

1. **Пароли хранятся в открытом виде**
   - Сделано по требованию для просмотра администратором
   - Необходим максимальный уровень защиты базы данных
   - Рекомендуется:
     - SSL-соединение с БД
     - Firewall rules для ограничения доступа
     - VPN для удаленного подключения
     - Регулярный аудит доступа

2. **Токены верификации**
   - Используется `secrets.token_urlsafe(32)` для генерации
   - Криптографически стойкие токены

3. **Email администратора**
   - Уведомления отправляются на `smtp_from_email` (можно настроить отдельный admin email)

## 📝 Файлы изменены/созданы

### Изменены:
- `app/core/config.py` - добавлены настройки SMTP
- `app/database/database.py` - новая таблица, изменения в User
- `app/database/user_db.py` - убрано хеширование
- `app/database/admin_db.py` - новые методы, убрано хеширование
- `app/routes/auth.py` - маршруты регистрации
- `app/routes/admin.py` - маршруты одобрения
- `app/templates/auth/register.html` - форма заявки
- `app/templates/auth/login.html` - ссылка на регистрацию
- `app/templates/admin/dashboard.html` - новые секции

### Созданы:
- `app/database/registration_db.py` - репозиторий заявок
- `app/templates/auth/register_success.html`
- `app/templates/auth/email_verified.html`
- `app/templates/admin/pending_registrations.html`
- `app/templates/admin/candidates.html`
- `app/templates/admin/sverkas.html`
- `MIGRATION_GUIDE.md` - инструкции по миграции

## 🚀 Быстрый старт

1. Обновить `.env` с настройками SMTP
2. Применить миграцию БД (см. `MIGRATION_GUIDE.md`)
3. Создать первого администратора: `POST /admin/setup-initial-admin`
4. Войти как администратор на `/admin/login`
5. Проверить регистрацию нового пользователя

## 🐛 Тестирование

### Тест-кейс 1: Регистрация пользователя
1. Открыть `/auth/register`
2. Ввести email и пароль
3. Проверить письмо
4. Перейти по ссылке
5. Войти как админ
6. Одобрить заявку в `/admin/pending-registrations`
7. Проверить письмо пользователю
8. Войти под пользователем на `/auth/login`

### Тест-кейс 2: Отклонение заявки
1-4. То же что в тест-кейсе 1
5. Отклонить заявку
6. Проверить что пользователь получил письмо об отклонении
7. Убедиться что пользователь не может войти

### Тест-кейс 3: Просмотр данных администратором
1. Войти как админ
2. Перейти в `/admin/candidates`
3. Убедиться что видны все кандидаты
4. Перейти в `/admin/sverkas`
5. Убедиться что видны все сверки
6. Перейти в `/admin/dashboard`
7. Убедиться что видны пароли всех рекрутеров

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера (print-сообщения с префиксами `[REGISTRATION]`, `[ADMIN]`)
2. Проверьте настройки SMTP в `.env`
3. Убедитесь что таблица `registration_requests` создана
4. Проверьте что поле `password` существует в таблице `users`

