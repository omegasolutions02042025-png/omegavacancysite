# Alembic - Быстрый старт

## ✅ Что сделано

1. **Установлен Alembic**
   ```bash
   pip install alembic
   ```

2. **Создана структура проекта**
   ```
   alembic/
   ├── versions/
   │   └── 20241202_1530-add_archive_status_fields.py
   ├── env.py
   ├── script.py.mako
   └── README
   alembic.ini
   ```

3. **Применена миграция архивации**
   ```bash
   alembic upgrade head
   ```

## 🚀 Основные команды

### Создать миграцию (автоматически)
```bash
alembic revision --autogenerate -m "Описание изменений"
```

### Применить миграции
```bash
alembic upgrade head
```

### Откатить миграцию
```bash
alembic downgrade -1
```

### Проверить текущую версию
```bash
alembic current
```

### Посмотреть историю
```bash
alembic history
```

## 📝 Типичный workflow

### 1. Изменение модели

Отредактируйте `app/database/database.py`:
```python
class User(SQLModel, table=True):
    # Добавьте новое поле
    new_field: Optional[str] = Field(default=None)
```

### 2. Создание миграции

```bash
alembic revision --autogenerate -m "Add new_field to users"
```

### 3. Проверка миграции

Откройте созданный файл в `alembic/versions/` и проверьте:
- Правильность изменений в `upgrade()`
- Корректность отката в `downgrade()`

### 4. Применение

```bash
alembic upgrade head
```

### 5. Перезапуск сервера

```bash
# Остановите сервер (Ctrl+C)
# Запустите заново
uvicorn app.main:app --reload
```

## ⚠️ Важно

- **Всегда проверяйте** автогенерированные миграции
- **Тестируйте** upgrade и downgrade перед продакшеном
- **Коммитьте** файлы миграций в Git
- **Не редактируйте** примененные миграции

## 🔧 Текущая миграция

**Файл:** `alembic/versions/20241202_1530-add_archive_status_fields.py`

**Что добавлено:**
- `is_archived` - статус архива (boolean)
- `archived_at` - дата архивации (varchar)
- `archived_by_admin` - ID администратора (integer, FK)

**Статус:** ✅ Применена

**Откат (если нужно):**
```bash
alembic downgrade -1
```

## 📚 Полная документация

См. файл `ALEMBIC_GUIDE.md` для подробной информации.

## 🆘 Помощь

**Проблема:** Миграция не применяется

**Решение:**
```bash
# Проверьте текущую версию
alembic current

# Посмотрите SQL без выполнения
alembic upgrade head --sql

# Проверьте подключение к БД
```

**Проблема:** Конфликт версий

**Решение:**
```bash
# Синхронизируйте с БД
alembic stamp head
```

---

**Следующий шаг:** Перезапустите сервер FastAPI!

