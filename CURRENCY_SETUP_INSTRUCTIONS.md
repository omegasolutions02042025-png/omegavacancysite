# Инструкция по установке и запуску модуля валют

## 📋 Предварительные требования

- Python 3.10+
- PostgreSQL (или SQLite для разработки)
- Доступ к интернету (для получения курсов ЦБ РФ)

## 🚀 Пошаговая установка

### Шаг 1: Установка зависимостей

```bash
# Активируйте виртуальное окружение (если используется)
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите новые зависимости
pip install requests==2.31.0 beautifulsoup4==4.12.2

# Или установите все зависимости из requirements.txt
pip install -r requirements.txt
```

### Шаг 2: Миграция базы данных

```bash
# Запустите скрипт миграции
python scripts/migrate_add_currency_fields.py
```

Ожидаемый вывод:
```
=== Начало миграции: добавление полей валют ===

1. Создание таблицы exchange_rates...
✓ Таблица exchange_rates создана

2. Добавление полей валют в candidate_profiles...
  ✓ Добавлено поле: base_rate_amount
  ✓ Добавлено поле: base_rate_currency
  ✓ Добавлено поле: rate_type
  ✓ Добавлено поле: rate_rub
  ✓ Добавлено поле: rate_usd
  ✓ Добавлено поле: rate_eur
  ✓ Добавлено поле: rate_byn
  ✓ Добавлено поле: rates_calculated_at
  ✓ Добавлено поле: exchange_rate_snapshot_id

=== Миграция завершена успешно! ===
```

### Шаг 3: Тестирование модуля

```bash
# Запустите тестовый скрипт
python scripts/test_currency_module.py
```

Ожидаемый вывод:
```
============================================================
ТЕСТИРОВАНИЕ МОДУЛЯ ВАЛЮТ
============================================================

============================================================
ТЕСТ 1: Парсер курсов ЦБ РФ
============================================================
✅ Парсер работает!
   USD: 95.5000 RUB
   EUR: 103.2500 RUB
   BYN: 29.8000 RUB

============================================================
ТЕСТ 2: Операции с базой данных
============================================================
📝 Создание записи с курсами...
✅ Запись создана (ID: 1)
   Время: 2025-12-01T10:30:00
   Активна: True

📊 Получение активного курса...
✅ Активный курс найден (ID: 1)
   USD: 95.5000
   EUR: 103.2500
   BYN: 29.8000

[... остальные тесты ...]

============================================================
ИТОГИ ТЕСТИРОВАНИЯ
============================================================
✅ PASS - Парсер курсов
✅ PASS - Операции с БД
✅ PASS - Конвертация валют
✅ PASS - Обновление курсов
✅ PASS - Проверка доступности

------------------------------------------------------------
Пройдено: 5/5
Успешность: 100.0%
============================================================

🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!
Модуль валют готов к использованию.
```

### Шаг 4: Запуск приложения

```bash
# Запустите приложение
uvicorn app.main:app --reload
```

Ожидаемый вывод:
```
INFO:     Will watch for changes in these directories: [...]
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [...]
[LIFESPAN] Exchange rates initialized
[SCHEDULER] Планировщик запущен
[SCHEDULER] - daily_folder_cleanup: каждый день в 03:00
[SCHEDULER] - daily_exchange_rates_update: каждый день в 09:00
[LIFESPAN] startup complete, email listeners started
INFO:     Application startup complete.
```

### Шаг 5: Проверка работы API

#### Проверка курсов валют

```bash
curl http://localhost:8000/api/currency/rates/current
```

Ожидаемый ответ:
```json
{
  "id": 1,
  "usd_rate": 95.50,
  "eur_rate": 103.25,
  "byn_rate": 29.80,
  "fetched_at": "2025-12-01T09:00:00",
  "is_active": true,
  "last_update_status": "success",
  "error_message": null
}
```

#### Тест конвертации

```bash
curl -X POST http://localhost:8000/api/currency/convert \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 1000,
    "from_currency": "USD",
    "to_currency": "RUB"
  }'
```

Ожидаемый ответ:
```json
{
  "original_amount": 1000,
  "from_currency": "USD",
  "converted_amount": 95500.00,
  "to_currency": "RUB",
  "exchange_rate_used": 95.5,
  "calculated_at": "2025-12-01T10:30:00"
}
```

#### Тест расчета ставки

```bash
curl -X POST http://localhost:8000/api/currency/calculate-rates \
  -H "Content-Type: application/json" \
  -d '{
    "base_amount": 3000,
    "base_currency": "USD",
    "rate_type": "monthly"
  }'
```

Ожидаемый ответ:
```json
{
  "base_amount": 3000,
  "base_currency": "USD",
  "rate_type": "monthly",
  "is_base": true,
  "rate_rub": 286500.00,
  "rate_usd": 3000.00,
  "rate_eur": 2775.18,
  "rate_byn": 9614.09,
  "rates_calculated_at": "2025-12-01T10:30:00",
  "exchange_rate_snapshot_id": 1,
  "exchange_rate_fetched_at": "2025-12-01T09:00:00"
}
```

### Шаг 6: Проверка документации API

Откройте в браузере:
```
http://localhost:8000/docs
```

Вы увидите автоматически сгенерированную документацию Swagger с разделом **currency**.

## 🔧 Настройка

### Изменение времени обновления курсов

Отредактируйте `app/core/scheduler.py`:

```python
# Изменить с 09:00 на 12:00
scheduler.add_job(
    update_exchange_rates_job,
    trigger=CronTrigger(hour=12, minute=0),  # <-- здесь
    id="daily_exchange_rates_update",
    replace_existing=True,
)
```

### Изменение источника курсов

Отредактируйте `app/core/exchange_rate_parser.py`:

```python
def parse_cb_rf():
    # Замените URL на другой источник
    url = 'https://your-api.com/rates'
    # Измените логику парсинга
    ...
```

## 📊 Интеграция в существующий код

### Обновление профиля кандидата

Найдите место, где создается/обновляется кандидат, и добавьте:

```python
from app.crud.candidate_rate import CandidateRateCRUD

# После создания/обновления кандидата
if candidate.base_rate_amount:
    await CandidateRateCRUD.update_candidate_rate(
        session,
        candidate.id,
        candidate.base_rate_amount,
        candidate.base_rate_currency or "RUB",
        candidate.rate_type or "monthly"
    )
```

### Добавление виджета в шаблон

В файле `app/templates/candidate/candidate_profile.html` добавьте:

```html
<!-- Где-то в профиле кандидата -->
<div class="candidate-rates-section">
    {% include "candidate/candidate_rates_widget.html" %}
</div>
```

И передайте данные в контроллере:

```python
from app.crud.candidate_rate import CandidateRateCRUD

# В функции отображения профиля
candidate = await CandidateRateCRUD.get_candidate_with_rates(session, candidate_id)

return templates.TemplateResponse("candidate/candidate_profile.html", {
    "request": request,
    "candidate_id": candidate.id,
    "base_rate_amount": candidate.base_rate_amount,
    "base_rate_currency": candidate.base_rate_currency,
    "rate_type": candidate.rate_type,
    "rate_rub": candidate.rate_rub,
    "rate_usd": candidate.rate_usd,
    "rate_eur": candidate.rate_eur,
    "rate_byn": candidate.rate_byn,
    "rates_calculated_at": candidate.rates_calculated_at,
})
```

## 🐛 Troubleshooting

### Проблема: Ошибка при миграции

**Симптомы:**
```
ERROR: relation "exchange_rates" already exists
```

**Решение:**
Таблица уже существует. Это нормально при повторном запуске миграции.

---

**Симптомы:**
```
ERROR: could not connect to database
```

**Решение:**
1. Проверьте, запущена ли база данных
2. Проверьте настройки в `app/core/config.py`
3. Проверьте переменную окружения `DATABASE_URL`

### Проблема: Курсы не загружаются

**Симптомы:**
```
Error parsing cb_rf: ...
```

**Решение:**
1. Проверьте доступность сайта ЦБ РФ:
   ```bash
   curl https://www.cbr.ru/currency_base/daily/
   ```

2. Проверьте интернет-соединение

3. Принудительно обновите курсы:
   ```bash
   curl -X POST http://localhost:8000/api/currency/rates/refresh
   ```

### Проблема: Scheduler не запускается

**Симптомы:**
```
[SCHEDULER] не появляется в логах
```

**Решение:**
1. Проверьте, что в `app/main.py` раскомментирована строка:
   ```python
   scheduler = start_scheduler()
   ```

2. Проверьте установку APScheduler:
   ```bash
   pip install apscheduler
   ```

### Проблема: Ставки не пересчитываются

**Симптомы:**
Старые значения ставок не обновляются

**Решение:**
1. Пересчитайте вручную через API:
   ```bash
   curl -X POST http://localhost:8000/api/currency/candidates/{id}/rate/recalculate
   ```

2. Пересчитайте все ставки:
   ```bash
   curl -X POST http://localhost:8000/api/currency/candidates/recalculate-all
   ```

## 📚 Дополнительная документация

- [CURRENCY_MODULE.md](CURRENCY_MODULE.md) - Полная документация модуля
- [CURRENCY_QUICKSTART.md](CURRENCY_QUICKSTART.md) - Быстрый старт
- [CURRENCY_IMPLEMENTATION_SUMMARY.md](CURRENCY_IMPLEMENTATION_SUMMARY.md) - Итоговая сводка

## ✅ Чек-лист установки

- [ ] Установлены зависимости (`requests`, `beautifulsoup4`)
- [ ] Выполнена миграция БД
- [ ] Пройдены все тесты
- [ ] Приложение запускается без ошибок
- [ ] API курсов валют работает
- [ ] Scheduler запущен
- [ ] Виджет интегрирован в UI (опционально)

## 🎉 Готово!

Модуль валют установлен и готов к использованию!

Для получения помощи:
1. Проверьте документацию
2. Запустите тесты
3. Проверьте логи приложения
4. Обратитесь к разработчикам

---

**Дата:** 01.12.2025
**Версия:** 1.0.0

