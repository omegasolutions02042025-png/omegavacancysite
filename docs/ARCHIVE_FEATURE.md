# Функция архивации пользователей

## Описание

Функция архивации позволяет администраторам блокировать доступ пользователей (рекрутеров) к системе без удаления их данных. Пользователи в архиве не могут войти в систему, но вся их история (кандидаты, сверки, чаты) сохраняется.

## Основные возможности

### Для администраторов

1. **Перевод в архив**
   - Блокирует вход пользователя в систему
   - Сохраняет все данные пользователя
   - Отправляет уведомление на email
   - Записывает дату и ID администратора

2. **Восстановление из архива**
   - Разблокирует доступ пользователя
   - Отправляет уведомление о восстановлении
   - Очищает метаданные архивации

3. **Визуальная индикация**
   - Архивные пользователи выделены серым цветом
   - Показан статус "Архив" в таблице
   - Ограниченный набор действий для архивных пользователей

### Для пользователей

- При попытке входа архивный пользователь получает сообщение:
  > "Ваш аккаунт находится в архиве. Обратитесь к администратору для восстановления доступа."
- Получение email уведомлений при архивации/восстановлении

## Структура базы данных

### Новые поля в таблице `users`

```sql
is_archived BOOLEAN DEFAULT FALSE
  -- Статус архива (True = в архиве, False = активен)

archived_at VARCHAR
  -- Дата и время перевода в архив (ISO format)

archived_by_admin INTEGER
  -- ID администратора, который перевел в архив
  -- FOREIGN KEY -> admins(id)
```

## API Endpoints

### POST /admin/archive-user
Перевести пользователя в архив.

**Параметры:**
- `user_id` (Form, required) - ID пользователя

**Ответ:**
```json
{
  "success": true,
  "message": "Пользователь переведен в архив"
}
```

**Требования:**
- Администратор должен быть авторизован
- Пользователь должен существовать

---

### POST /admin/unarchive-user
Восстановить пользователя из архива.

**Параметры:**
- `user_id` (Form, required) - ID пользователя

**Ответ:**
```json
{
  "success": true,
  "message": "Пользователь восстановлен из архива"
}
```

**Требования:**
- Администратор должен быть авторизован
- Пользователь должен существовать

## Методы репозитория

### UserRepository.archive_user(user_id, admin_id)
Перевести пользователя в архив.

**Параметры:**
- `user_id: int` - ID пользователя
- `admin_id: int` - ID администратора

**Возвращает:**
- `User | None` - Обновленный пользователь или None

**Действия:**
- Устанавливает `is_archived = True`
- Записывает текущее время в `archived_at`
- Записывает ID администратора в `archived_by_admin`

---

### UserRepository.unarchive_user(user_id)
Восстановить пользователя из архива.

**Параметры:**
- `user_id: int` - ID пользователя

**Возвращает:**
- `User | None` - Обновленный пользователь или None

**Действия:**
- Устанавливает `is_archived = False`
- Очищает `archived_at = None`
- Очищает `archived_by_admin = None`

## Логика аутентификации

В `app/routes/auth.py` добавлена проверка статуса архива:

```python
# После успешной аутентификации
if user.is_archived:
    raise HTTPException(
        status_code=403, 
        detail="Ваш аккаунт находится в архиве..."
    )
```

**HTTP коды:**
- `403 Forbidden` - Пользователь в архиве
- `400 Bad Request` - Неверные учетные данные

## Интерфейс администратора

### Панель управления (`/admin/dashboard`)

**Для активных пользователей:**
- 🔐 Доступ (управление заказчиками)
- 🔑 Пароль (смена пароля)
- 📊 Сверки (история сверок)
- 👥 Кандидаты (список кандидатов)
- 🗄️ Архив (перевести в архив)

**Для архивных пользователей:**
- ↩️ Восстановить (восстановить из архива)
- 📊 Сверки (история сверок - только просмотр)
- 👥 Кандидаты (список кандидатов - только просмотр)

### Визуальные индикаторы

- Строка архивного пользователя имеет серый фон (`class="table-secondary"`)
- Email дополнен меткой "🗄️ В архиве"
- Статус показан как красный badge "Архив"

## Email уведомления

### При архивации
```
Тема: Аккаунт переведен в архив - OmegaHire

Ваш аккаунт в системе OmegaHire был переведен в архив администратором.
Вы больше не можете войти в систему.
```

### При восстановлении
```
Тема: Аккаунт восстановлен - OmegaHire

Ваш аккаунт в системе OmegaHire был восстановлен из архива администратором.
Теперь вы снова можете войти в систему, используя свои учетные данные.
```

## Миграция базы данных

Для добавления новых полей в существующую базу данных:

```bash
python scripts/migrate_add_archive_status.py
```

**Что делает миграция:**
1. Проверяет существование колонок
2. Добавляет `is_archived` (BOOLEAN, default FALSE)
3. Добавляет `archived_at` (VARCHAR, nullable)
4. Добавляет `archived_by_admin` (INTEGER, FK to admins.id)
5. Устанавливает всем существующим пользователям статус "Активен"

## Примеры использования

### JavaScript (Frontend)

```javascript
// Перевести в архив
async function archiveUser(userId, userEmail) {
  if (!confirm(`Перевести ${userEmail} в архив?`)) return;
  
  const formData = new FormData();
  formData.append('user_id', userId);
  
  const response = await fetch('/admin/archive-user', {
    method: 'POST',
    body: formData
  });
  
  const data = await response.json();
  if (data.success) {
    alert('✅ Пользователь переведен в архив!');
    window.location.reload();
  }
}

// Восстановить из архива
async function unarchiveUser(userId, userEmail) {
  if (!confirm(`Восстановить ${userEmail} из архива?`)) return;
  
  const formData = new FormData();
  formData.append('user_id', userId);
  
  const response = await fetch('/admin/unarchive-user', {
    method: 'POST',
    body: formData
  });
  
  const data = await response.json();
  if (data.success) {
    alert('✅ Пользователь восстановлен!');
    window.location.reload();
  }
}
```

### Python (Backend)

```python
from app.database.user_db import UserRepository

user_repo = UserRepository()

# Перевести в архив
user = await user_repo.archive_user(user_id=5, admin_id=1)
if user:
    print(f"Пользователь {user.email} переведен в архив")

# Восстановить из архива
user = await user_repo.unarchive_user(user_id=5)
if user:
    print(f"Пользователь {user.email} восстановлен")

# Проверить статус при входе
user = await user_repo.authenticate(email, password)
if user and user.is_archived:
    raise HTTPException(status_code=403, detail="Аккаунт в архиве")
```

## Безопасность

1. **Авторизация**: Все операции требуют авторизации администратора
2. **Аудит**: Записывается ID администратора и время архивации
3. **Сохранность данных**: Данные пользователя не удаляются
4. **Email уведомления**: Пользователь информируется о изменениях

## Часто задаваемые вопросы

### Что происходит с данными архивного пользователя?
Все данные сохраняются: кандидаты, сверки, чаты, история. Администратор может просматривать их через панель.

### Можно ли удалить пользователя вместо архивации?
Функция удаления не реализована намеренно. Архивация - более безопасный способ.

### Получит ли пользователь уведомление?
Да, на email отправляется уведомление при архивации и восстановлении.

### Можно ли изменить пароль архивного пользователя?
Нет, для архивных пользователей доступны только просмотр данных и восстановление.

### Что видит пользователь при попытке входа?
Сообщение: "Ваш аккаунт находится в архиве. Обратитесь к администратору для восстановления доступа."

## Changelog

### v1.0.0 (2024-12-02)
- ✨ Добавлена функция архивации пользователей
- ✨ Добавлена функция восстановления из архива
- ✨ Блокировка входа для архивных пользователей
- ✨ Email уведомления при архивации/восстановлении
- ✨ Визуальная индикация в админ-панели
- ✨ Аудит действий (кто и когда перевел в архив)
- 📝 Документация и миграция базы данных

